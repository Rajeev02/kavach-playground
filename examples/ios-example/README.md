# Kavach SDK - iOS Integration

This directory contains a skeleton integration of the Kavach iOS SDK using Swift.

## Setup

1. Add the Kavach SDK to your `Podfile`:
```ruby
pod 'KavachSDK'
```

2. Install the pods:
```bash
pod install
```

3. Open the `.xcworkspace` and initialize the SDK in your `AppDelegate.swift`.
