# Kavach SDK - Flutter Integration

This directory contains a skeleton integration of the Kavach Flutter SDK using Dart.

## Setup

1. Add the Kavach SDK to your `pubspec.yaml`:
```yaml
dependencies:
  kavach_flutter: ^1.0.0
```

2. Get dependencies:
```bash
flutter pub get
```

3. Initialize the SDK in your `main.dart`.
